# .env.example (template file - safe to commit)
# /The original .env is not committed

OPENWEATHER_API_KEY=api_key_here
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=password_here
DB_NAME=weather_analytics
